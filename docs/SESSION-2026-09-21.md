# SESSION — 2026-09-21 — Hero V3 final + deploy prep

> Session notes biar next session langsung nyambung. Baca ini + `docs/PRD-ADDENDUM-OPencode.md` dulu.

## TL;DR session ini

1. **Fix crash load-bearing** — `ManifestoSection` `useTransform` input range sampe 1.25 → WAAPI `Offsets must be in range [0,1]` → React 19 unmount ROOT APP (root empty, 0 section). Fix: 3-stop range `[start, mid, end]`.
2. **Fix duplicate React keys** — AchievementsSection `key={ach.label}` → `stat-${i}`; CaseStudySection `key={m.label}` → `metric-${i}`.
3. **Hero re-center** — person di exact 50% (768px @1536). Right cluster geser kanan. 61px clearance, 0 alpha-pixel contact.
4. **Journey rewrite** — jadi "Process Blueprint" (3×2 node grid, SVG serpentine route, stages DISCOVER→LEARN→BUILD→TEST→SHIP→GROW). `journey.ts` → `processStages`.
5. **Skills rewrite** — jadi "Technical Workbench" (workflow strip + 6 capability modules + hover detail panel). `skills.ts` → `capabilityModules`.
6. **Manifesto V3** — KEEP LEARNING / BUILD WITH PURPOSE / MAKE IT REAL / KEEP GOING.
7. **Hero V3 final correction** (person LOCKED):
   - Title center `left-1/2 -translate-x-1/2`, `letterSpacing 0.06em`, white halo di extrude shadow.
   - **Slogan split-flank** (`SloganFlanks` di WijayaTitle.tsx): kiri `right-[calc(50%+112px)]`, kanan `left-[calc(50%+60px)]`. Gap 172px ngikutin muka. Alpha 26% → **0%** di semua width. <1521px: kiri 2 baris, kanan tinggal "DREAMS."
   - **About-card relokasi** <1536px: ke kolom kanan `right-[40px] top-[520px]` max-w-240. Alpha 76%→**0%** di 1280/1330/1366/1440/1520. Quote hidden <1536, caption geser `right-[306px] top-[548px]`.
   - **Breakpoint desktop 1024→1280** (`min-[1280px]`). <1280 dapet stacked layout utuh.
   - Picker `max-[1299px]:hidden` (hidden <1300), `max-[1535px]:right-[370px]`.
8. **Deploy prep** — `vercel.json`, `.gitignore`, README rewrite, git init.

## Geometry final (1536×864)

- title x483 y56 w571 h149 (center 768.5)
- slogan: flank-kiri x439 w217, flank-kanan x828 w296 (gap muka di tengah)
- device x1178 y220 w318 h288 | about x990 y597 w330 h167 (≥1536)
- about <1536: `right-[40px] top-[520px]` w240 h216
- profile x72 y424 w284 h186 | focus x72 y344 | social x35 y150
- person x544 y67 w448 h800 (img→screen: ix=544 iy=67 iw=448 ih=800, asset 717×1280)
- Person clearance: about xgap 136–256px (<1536), 0 overlap semua width.

## QA gates (semua PASS)

- tsc ✓, lint ✓ (2 pre-existing `react(set-state-in-effect)` warnings), build ✓ 449KB JS / gzip 136KB
- 0 console error, 0 page error, 0 overflow (scrollW == innerW) di 1280/1330/1366/1440/1520/1536/1728/1920
- Alpha collision: 0% (slogan, about, picker, device, quote, caption, profile, focus, keep, motto)
- 12/12 section render, lazy-image "broken" = false positive (display:none / carousel)

## Yang BELUM kelar (next session priority)

- **Vercel deploy**: `vercel.json` siap. Belum ada `VERCEL_TOKEN` di env → `npx vercel login` (browser) atau import via dashboard. Repo: `sukmawijaya160208-a11y/fotofolio-wijaya`.
- Vercel project di-rename 2x: fotofolio-wijaya → sukma-wijaya → **muhammad-sukma-wijjaya** (ikut tulisan user, double-J — flag kalo typo).
- Domain live: **https://muhammad-sukma-wijjaya.vercel.app** (200, title bener, QA 12/12 @1366, 0 err, 0 overflow).
- GOTCHA Vercel: `ssoProtection.deploymentType=all_except_custom_domains` bikin SEMUA `.vercel.app` redirect ke login. Fix: PATCH /v9/projects/{id} `{"ssoProtection":null}` (token dari CLI auth.json, jangan print). Cek: GET project → ssoProtection.
- Alias lama (fotofolio-*, sukma-*) dihapus via `vercel alias rm`. Alias aktif: bare + `-yah-0713` suffix.
- Token CLI di `$XDG_DATA_HOME/com.vercel.cli/auth.json` (env ini XDG_DATA_HOME custom ke snap path!).
- **Git push**: `git init` done, identity perlu set (`sukmawijaya160208-a11y` + noreply email), repo belum dibuat di GitHub.
- **Konten placeholder** — masih kosong, blocks "looks finished":
  - `projects.ts` → "PROJEK NAMA" (4 slot)
  - `education.ts` → "XX" (placeholder tahun/institusi)
  - `achievements.ts` → "99+" (stats, sertifikat 4 slot)
  - `gallery.ts` → "SPECIMEN NN" (6 slot)
  - `case-study.ts` → metric label kosong
- **Assets**: `public/assets/*.webp` (project-01..04, gallery-01..06, certificate-01..04, case-study) masih generated placeholder 9-10KB — perlu replace foto/desain asli.

## Tools / gotcha penting

- **Model GAK BISA liat gambar yang di-paste di chat** (`[Image 1]` = placeholder kosong). Tapi **bisa baca file gambar** lewat tool Read. Kalau user kirim screenshot, minta save jadi file (`/tmp/xxx.png`) baru kasih path-nya.
- Headless Chromium: `img.decode()` bisa hang di lazy-image dalam `display:none` (MobileStage). Jangan pernah `await` tanpa timeout. Race condition, bukan bug produk.
- Screenshot person kadang "hilang" di headless = paint flake (compositor race), bukan bug. Selalu croscek `naturalWidth` + opacity sebelum panik.
- Feedback protocol (`docs/PRD-ADDENDUM-OPencode.md` §4): user kasih `fix: [section] [elemen] [masalah] [koordinat]` → jawab before→after angka + re-screenshot.
- Dev server: `http://127.0.0.1:5199/`. Cek live: `curl -s -o /dev/null -w "%{http_code}" :5199/`.
- Python di env ini GAK ada playwright-nya → pakai Node (`node script.mjs`) dari cwd proyek (node_modules ada playwright).
- Tailwind 4: arbitrary variant `min-[1280px]:` / `max-[1535px]:` dipakai untuk breakpoint custom.

## Cara kerja verified (jangan diulang)

1. Ubah layout → dump geometry (getBoundingClientRect semua elemen).
2. Alpha-collision test: draw img ke canvas, baca `getImageData`, sample overlap region dgn person alpha >40.
3. Cek `scrollW == innerW` (overflow), gap matrix antar elemen (signed).
4. Test di 1280/1330/1366/1440/1520/1536/1728/1920, bukan cuma 1536.
5. Baru klaim kelar. **Jangan verify cuma 1 width** (kemarin kepedean, about 76% overlap lolos).

## Files penting

- `src/components/hero/HeroSection.tsx` — desktop stage absolute positions
- `src/components/hero/WijayaTitle.tsx` — title + Slogan + **SloganFlanks**
- `src/components/hero/HeroPerson.tsx` — immutable photo wrapper
- `src/data/assets.ts` — asset registry (heroWidth/Height)
- `docs/PRD-ADDENDUM-OPencode.md` — numeric gates + prohibitions
- `vercel.json` / `.gitignore` — deploy config

## V5 REFINEMENT PASS (PRD "VISUAL REFINEMENT V5") — 6 pass selesai

- **PASS 1 Hero**: nemu & fix bug MountainCard collapse (wrapper absolute shrink-to-fit → 2px width, kartu invisible). Hapus RightQuoteCard (redundan vs DeviceCaption "SAME BOY BIGGER VISION"; PRD §4.6 "Best Version" = label kecil bukan kartu). B-07 + crosshair kanan tadinya ketimbun di belakang IdentityDevice → pindah ke dinding kosong. Z-layer plan didokumentasi. Audit geometry 1536/1440/1280: 0 tabrakan real.
- **PASS 2 Section 07**: 3 → 4 fase (PROBLEM/APPROACH/BUILD/RESULT), BUILD jadi numbered process steps (data `process[]`), MetaRow ROLE/STACK/DURATION, blueprint overlay (grid + corner + "CASE / 01 · FRAME 16:9" + "IMAGE SLOT — ASSET PENDING"), CTA "ALL PROJECTS →" (anchor #projects, no fake link).
- **PASS 3 Section 11 CODE UNIVERSE**: three + R3F + drei (install `--legacy-peer-deps` — fiber 9.7 cap react <19.3, project react 19.3.0; aman web). 17 node dari data real (skills.ts), 5 orbit, camera journey scroll-driven (z 17→4.2 + arc), HUD blueprint, hover scale 1.1 + label fade, click → info panel, lazy-mount via IntersectionObserver, dpr/star tier mobile, reduced-motion → statik. Chunk three.js terpisah 908KB lazy; initial bundle tetap 454KB.
- **PASS 4 Section 12**: form MAILTO beneran (no backend, no fake submit), validasi name/email/message, state loading→sent, CopyEmailButton (clipboard + COPIED state), local time Asia/Jakarta live tick, social rows hover arrow.
- **PASS 5 Section 13**: monogram SVG stroke-draw 1.4s (bukan fake signature — diizinkan §27), "THANK YOU. / KEEP BUILDING.", END OF TRANSMISSION + metadata truthful (REACT, VER 2.4).
- **PASS 6 QA**: 8 viewport (1536/1440/1280/1024/768/430/390/375) → 0 console error, 0 page error, 0 horizontal overflow, 12 section, canvas+WebGL mount, person 717px. oxlint: warning doang (Math.random di StarField — dalam useMemo, stabil).
- **GOTCHA test**: `scrollIntoView()` + `scrollBy()` cepat dengan `scroll-behavior:smooth` saling cancel → scrollY diam di tempat. QA script wajib set `scrollBehavior='auto'` dulu.

## V5 DEPLOY GOTCHA (penting!)
- `vercel --prod` sukses, tapi alias bare `muhammad-sukma-wijjaya.vercel.app` TETAP nunjuk deploy lama — karena alias di-set manual di sesi sebelumnya (manual alias gak auto-follow production deploy baru).
- Gejalanya: deploy URL baru ada kode V5, tapi alias masih kode V4 ("11 / MANIFESTO", "TERMINAL", no universe). Jangan tertipu QA di alias lama!
- Fix: `vercel alias set <deploy-url-baru> muhammad-sukma-wijjaya.vercel.app`.
- PROD final: https://muhammad-sukma-wijjaya.vercel.app — 200, 12 section, 0 error, 0 overflow, V5 live.

## V5.1 — REAL SOLAR SYSTEM (user request)
- Section 11 manifesto text sequence (KEEP LEARNING / BUILD WITH PURPOSE / ...) DIHAPUS — section = solar 3D scroll only. Label "11 / CODE UNIVERSE" tetap.
- Planet sekarang tekstur NASA beneran (threex.planets, self-host di public/assets/planets/, 13 file 2.8MB, lazy-loaded bareng universe chunk):
  React=Earth+clouds, JS=Jupiter, TS=Saturn+ring, Node=Mars, Python=Neptune, API=Venus,
  tools=Moon (tint beda), interests=Mercury/Uranus/Pluto, exploration=procedural ice.
- Matahari: sunmap + glow sprite (pulse subtle) + point light hangat. Saturn ring pakai RingGeometry + radial UV fix.
- BUG ketemu: orbit `* 8` → 1 putaran/14 detik (mabok + klik meleset). Fix: speed data mentah (0.02–0.055 rad/s → 1 putaran ~2–5 menit). Self-rotation 0.08–0.28 = "hidup" tapi tenang.
- SphereGeometry 36x24 (hi) / 20x14 (low). Hover raycast + click panel verified (VS Code/ORBIT 04).
- QA: 1536/1280/390 → 0 error, 0 overflow, 12 section.

## V6 — SOLAR SYSTEM OBSERVATORY (PRD MASTER v2.0)
- Section 11 diganti total: Code Universe lama HAPUS (components/universe/*, data/universe.ts deleted).
- Baru: components/solar/ (SolarSystemScene, SolarSystemSection, solarTheme) + data/solarSystem.ts + data/portfolioMap.ts.
- 8 planet beneran (Merkurius→Neptunus) + Matahari, tekstur self-host yg sudah ada. Tilt axial real (Uranus 97.8°!), Venus rotasi retrograde.
- Mapping jujur §14: experiences/projects/sertifikat = PLACEHOLDER → panel STATUS: PENDING, tanpa nama fake. Real: profile/skills/education/focus.
- Observatorium UI: MW/OBSERVATORY, SYSTEM ONLINE (lime #C7FF3D hanya utk state/seleksi, planet TIDAK di-tint hijau), 09 OBJECTS, ACTIVE TARGET, SCROLL TO EXPLORE, target cycler ◀ ▶.
- Panel: TARGET/CATEGORY/STATUS/RELATED + CTA ke anchor real (#skills/#projects/#work/#about/#hero/#contact/#creative-lab).
- Loader progress REAL (drei useProgress via callback) — INITIALIZING OBSERVATORY xx% → SYSTEM READY.
- Kamera: scroll overview + fokus dolly ke planet yg bergerak (world-pos per frame, easing), exit >0.88 kembali wide. No teleport.
- Keyboard: ←/→ ganti target (pas section visible), Esc tutup. ErrorBoundary → fallback DATA MODE. sr-only list + SEO text.
- Lenis DITOLAK: native scroll + camera lerp 0.06 sudah cinematic; Lenis global berisiko merusak animasi framer section lain.
- Bundle fix: import { LIME } dari scene narik three ke main (1.4MB!) → solarTheme.ts mungil. useProgress barrel drei +345KB → pindah ke callback dalam Canvas. Main 429KB, solar 910KB lazy.
- Quality: user minta HIGH di semua device (termasuk mobile). Touch: planet 1.3x biar gampang tap.
- QA: 8 viewport 0 error/0 overflow/canvas+WebGL mount; keyboard+Esc verified; hover/click panel verified.

## V6.1 — cleanup
- moonmap1k.jpg + plutomap1k.jpg dihapus (tidak dipakai mapping baru). 11/11 tekstur terpakai ke-load semua.
- Loader: 0% ~12s first-load lalu SYSTEM READY — progress real (bukan fake), wajar untuk 2.4MB tekstur.

## V6.2 — section 11 tanpa scroll-choreography (user request)
- User: konten section 11 keskip/gak keliatan karena animasi scroll 360vh. Solusi: section jadi statis h-[100svh] — solar + judul + HUD langsung tampil, tanpa timeline scroll.
- CameraRig: overview statis (angle 0.3, z 10.5) + fokus dolly pas klik planet. lerpKeys dihapus.
- Hint diganti "CLICK A PLANET TO FOCUS". Panel/keyboard/loader/fallback tetap.

## V6.3 — ULTRA: drag camera + tekstur 2K + Bima Sakti (user request)
- OrbitControls (drei): drag rotate + scroll zoom (pan OFF biar sistem tetap framing), damping 0.08. Rig vs controls: intro & transisi dolly milik rig (controls disabled), selebihnya milik user. Target ngikutin planet yg bergerak.
- Tekstur 2K Solar System Scope (CC-BY 4.0, atribusi di README + sr-only): sun/mercury/venus-atmosphere/earth-daymap+clouds+nightmap/mars/jupiter/saturn+ring-alpha/uranus/neptune + 2k_stars_milky_way. Total 5.5MB lazy. Tekstur 1K lama dihapus.
- Earth night lights (emissiveMap nightmap) — sisi malam nyala lampu kota. Anisotropy 8. Sky sphere Bima Sakti r=150 + drift pelan. far 400.
- QA: 13/13 tekstur ke-load, drag verified (pixel berubah), 0 error/0 overflow desktop+mobile.

## V6.4 — drag fix (user: "cuma bisa tekan, gak bisa geser")
- Root cause: DEADLOCK. controls.enabled=false di awal, nyalanya cuma via onStart — yg gak pernah fire karena controls mati. Plus rig narik kamera balik ke BASE tiap frame (rubber-band).
- Fix: rig HANYA nyetir kamera pas transisi dolly; selebihnya OrbitControls penuh (tanpa lookAt tandingan). Kamera start langsung di BASE (intro dolly dihapus — loader nutupin detik awal).
- Seleksi planet pindah onPointerDown → onClick (drag dari atas planet = rotate, bukan select).
- Html label dikasih wrapperClass solar-label + CSS pointer-events:none (overlay gak makan drag).
- Verified: drag ubah view 98.8%, settle = ambient (no snap-back), klik panel OK, 0 error.

## Sertifikat asli (user supply)
- 4 foto dari root (cut, bukan copy): Prompt Engineer / AI Prompt / Cyber Security / UI UX Designer → webp q80 max-1600 → public/assets/certificate-01..04.webp (timpa placeholder).
- Judul dari nama file; issuer/tahun = "—" (tidak dikarang, nunggu owner spill).
- QA: 4 img naturalWidth 1600, alt bener, no "CERTIFICATE TITLE", 0 404.
- Sisa placeholder bergambar: project-*.webp, gallery-*.webp, case-study.webp, achievements mini.

## Project index redesign (user: modern, no AI slop)
- Card grid generik (badge/gradient/tag-pill) DIGANTI editorial index list: hairline rows, nomor mono gold, judul display clamp(28-60px) + hover slide, meta kanan + panah.
- Floating image preview (desktop fine-pointer): ngikutin cursor via ref DOM langsung (no re-render), 4 img stacked crossfade. Mobile: inline 16:9 per baris.
- QA: 4 rows, 8 img loaded, preview opacity+swap bener, 0 overflow/error (1536+390).

## Project foto asli + sticky preview (user feedback)
- User: preview ngikutin cursor di laptop ganggu. Fix: cursor-following DIBUANG TOTAL → panel sticky kanan (top-110px, aspect 4/5, caption + nomor). Hover row = crossfade. 0 elemen translate() tersisa (verified).
- 4 foto asli dari root (cut): WEBSITE ANIME (featured) / UI UX DESAIN / WEBSITE ARSITEK GAME / WEB LESTARI BUMI → webp q82 (52-84KB) → project-01..04.webp. Judul+category real dari nama file; deskripsi "menyusul" (no ngarang).
- QA: 4 rows, 8 img loaded, swap bener (row3→img2), 0 error/overflow (1536+390).

## Section 07 case study asli (user pilih opsi A)
- Foto dari root (cut): "UNTUK SECITION 7 .jpeg" 735px → upscale lanczos 1440 + webp q85 (128K) → case-study.webp.
- caseStudies.ts diisi (izin owner, masuk akal, tanpa klaim klien/metrik): WEBSITE ANIME, role FRONTEND DEVELOPER, tools React/TS (konsisten project-01), duration "—", 4 fase narasi, metrics jujur (2026/01/ID).
- Overlay "IMAGE SLOT — ASSET PENDING" → "WEBSITE ANIME — 2026". project-01 description disinkronkan.
- QA: img 1440px load, 4 fase, no placeholder, 0 error (1536+390).

## Section 7 SHOWCASE 2 konten + migrasi bio (user supply)
- BIO (data owner, override): Sistem Informasi Smt 3, Univ Bina Insan Lubuklinggau, Prompt Engineer; IG @sukmawijaya1602, WA 0851-6988-3337, TikTok PRO'MT Sukma, FB Sukma Wijaya; fokus Coding/Graphic/Web Design. Email tetap (satu-satunya yg diketahui). TikTok/FB/Pinterest tanpa URL valid → tidak dipasang link (no fake links).
- Files: profile.ts, contact.ts (WA+IG+WEBSITE+EMAIL), education.ts, manifesto meta, achievements note, IdentityBadge PROMPT ENGINEER/SISTEM INFORMASI, footer meta direstore.
- SEC7: (A) VideoAccordion adaptasi elastic prompt (Vite, no next/image/shadcn): 5 YouTube click-to-play (nocookie, lazy), keyboard ok. (B) ServicesGlass adaptasi glass prompt: 3 layanan + status + socials + WA CTA, tanpa player/playlist fake.
- caseStudies.ts + cinematic lama DIHAPUS. Judul video netral (VIDEO 01-05, owner spill judul asli).
- QA: 5 panel + 5 thumb, iframe nocookie verified, bio baru render, no stale (PGRI-hit = false positive "SHIPGRID"), 0 error/overflow (1536+390).

## Section 7 konten-2: FOCUS MIX player (revisi, user marah bener)
- ServicesGlass DIBUANG (user mau kaya Spotify). Ganti ListenApp: adaptasi glass prompt (tanpa shadcn), tema dark+gold proyek.
- Semua kontrol REAL: klik track / prev-next ganti embed Spotify asli, like toggle lokal, Open in Spotify link. Progress/time/volume/shuffle/repeat FAKE dari prompt DIBUANG.
- playlist.ts: 4 track ID real, label SAMPLE PLAYLIST + cara ganti. QA: embed swap verified (Heat Waves → Levitating), 0 error (1536+390).

## Scroll-video Section 8+9 (user request)
- education-bg.mp4 + highlight.mp4 (1.7MB, 8s, 480p, faststart) dari root (cut) → public/assets/.
- Shared ScrollVideo: currentTime = scroll progress (offset start-end/end-start), lerp 0.12 via rAF, threshold seek 1/30s. Tidak pernah play() → lolos autoplay. Lazy src (rootMargin 800px), reduced-motion = frame statis, error → fallback bg.
- Sec8: konten UTUH, video bg + gradient tepi saja (tengah cerah + brightness 1.06, sesuai "jangan abu-abu").
- Sec9: achievements NUMBERS DIHAPUS → full video + overlay minimal (09/HIGHLIGHT/MOMEN + hint). id achievements dipertahankan.
- QA: scrub maju-mundur verified 1536+390, 0 error/overflow.
